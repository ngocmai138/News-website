@model IEnumerable<News.Models.Category>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<!-- # site header
================================================== -->
<header id="masthead" class="s-header">

    <div class="s-header__branding">
        <p class="site-title">
            <a href="/" rel="home">Spurgeon.</a>
        </p>
    </div>

    <div class="row s-header__navigation">
        <nav class="s-header__nav-wrap">

            <h3 class="s-header__nav-heading">Navigate to</h3>

            <ul class="s-header__nav">
                <li class="current-menu-item"><a href="/" title="">Home</a></li>
                <li class="has-children">
                    <a href="/Categories" title="" class="">Categories</a>
                    <ul class="sub-menu">
                        @foreach (var item in Model)
                        {
                            <li><a asp-controller="Categories" asp-action="Details" asp-route-id="@item.Id">@item.Name</a></li>
                        }
                    </ul>
                </li>
                <li><a href="/Articles" title="">Articles</a></li>
                @if (User.Identity.IsAuthenticated && User.IsInRole("Administrator"))
                {
                    <li><a href="/Articles" title="">Quản lý</a></li>
                }
                @if (!User.Identity.IsAuthenticated)
                {
                    <li><a href="/Users/Login" title="">Đăng nhập</a></li>
                }
                @if (User.Identity.IsAuthenticated)
                {
                    <li><a href="/Users/Logout" title="">Thoát</a></li>
                }
            </ul> <!-- end s-header__nav -->

        </nav> <!-- end s-header__nav-wrap -->
    </div> <!-- end s-header__navigation -->
    @await Component.InvokeAsync("Search")

    <a class="s-header__menu-toggle" href="#0"><span>Menu</span></a>
    <a class="s-header__search-trigger" href="#">
        <svg width="24" height="24" fill="none" viewBox="0 0 24 24">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19.25 19.25L15.5 15.5M4.75 11C4.75 7.54822 7.54822 4.75 11 4.75C14.4518 4.75 17.25 7.54822 17.25 11C17.25 14.4518 14.4518 17.25 11 17.25C7.54822 17.25 4.75 14.4518 4.75 11Z"></path>
        </svg>
    </a>

</header> <!-- end s-header -->
